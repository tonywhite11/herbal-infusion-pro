import { InfusionType } from './types';

export const GEMINI_MODEL_NAME = "gemini-2.5-flash-preview-04-17";

export const INFUSION_TYPE_OPTIONS: InfusionType[] = Object.values(InfusionType);

export const LOCAL_STORAGE_API_KEY = 'herbalInfusionAiApiKey';

export const DEFAULT_DISCLAIMER = "This recipe is for informational purposes only and not a substitute for professional medical advice. Consult with a healthcare provider before using herbal remedies, especially if you have pre-existing conditions, are taking medications, or are pregnant/nursing.";